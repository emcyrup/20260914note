# SITE_ADDRESS は compose が .env の DOMAIN から作る：ドメインなら自動HTTPS、空なら :80（IP直アクセス・検証用）
{$SITE_ADDRESS} {
	encode gzip

	# 日誌の写真などアップロードファイルは Caddy が直接配信する
	handle_path /media/* {
		root * /srv/media
		file_server
	}

	reverse_proxy app:8000
}
