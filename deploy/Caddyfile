# DOMAIN が設定されていればそのホスト名で自動HTTPS、なければ :80（IP直アクセス・検証用）
{$DOMAIN::80} {
	encode gzip

	# 日誌の写真などアップロードファイルは Caddy が直接配信する
	handle_path /media/* {
		root * /srv/media
		file_server
	}

	reverse_proxy app:8000
}
